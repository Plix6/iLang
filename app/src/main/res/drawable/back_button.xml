<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/primary"/> <!-- Background color for the circle -->
        </shape>
    </item>

    <!-- Image to be placed inside the circle -->
    <item android:drawable="@drawable/back_button_image"
        android:gravity="center"
        android:width="40dp"
        android:height="40dp"/>
</layer-list>
